apiVersion: v1
kind: Service
metadata:
    name: user
    namespace: roboshop-dev
    labels:
        project: roboshop
        tier: backend
        service: user
spec:
    selector:
        project: roboshop
        tier: backend
        service: user
    ports:
        - protocol: TCP
          port: 8080
          targetPort: 8080